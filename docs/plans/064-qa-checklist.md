# #64 - Supabase RLS 보안 강화 QA Checklist

> Generated by qa-generator agent
> Date: 2026-03-25

## 테스트 요약

- **총 테스트 케이스**: 28개
- **우선순위별**: High 12, Medium 11, Low 5
- **예상 테스트 시간**: 45분

---

## 기능 테스트

> 기존 사용자 흐름이 보안 변경 후에도 정상 동작하는지 확인합니다.
> 모든 API 라우트는 `supabaseAdmin`(service_role)을 사용하므로 RLS 우회됩니다.

| #     | 테스트 시나리오                  | 사전 조건              | 테스트 단계                                                               | 예상 결과                                                 | 우선순위 |
| ----- | -------------------------------- | ---------------------- | ------------------------------------------------------------------------- | --------------------------------------------------------- | -------- |
| FT-1  | 면접 세션 생성                   | 로그인 상태            | 1. 카테고리 선택<br>2. 질문 생성 요청<br>3. 면접 시작                     | 세션 및 질문이 정상 생성됨 (`sessions`, `questions` 쓰기) | High     |
| FT-2  | 질문 조회 및 답변 제출           | 진행 중인 세션 존재    | 1. 면접 세션 진입<br>2. 질문 확인<br>3. 답변 입력 후 제출                 | 답변 저장 및 피드백 반환                                  | High     |
| FT-3  | 질문 힌트 요청                   | 면접 세션 진행 중      | 1. 질문 화면에서 힌트 버튼 클릭                                           | 힌트가 정상 반환됨                                        | Medium   |
| FT-4  | 질문 교체 (replace)              | 면접 세션 진행 중      | 1. 질문 교체 버튼 클릭                                                    | 새 질문 생성 및 교체됨                                    | Medium   |
| FT-5  | 즐겨찾기 추가/삭제               | 로그인 상태            | 1. 질문에서 즐겨찾기 토글<br>2. 즐겨찾기 목록 확인                        | 즐겨찾기 추가/삭제 반영, 목록에 표시                      | High     |
| FT-6  | 답변 즐겨찾기                    | 답변 완료 상태         | 1. 답변 즐겨찾기 토글<br>2. 즐겨찾기 목록에서 확인                        | 답변 즐겨찾기 정상 작동                                   | Medium   |
| FT-7  | 세션 완료                        | 모든 질문 답변 완료    | 1. 세션 완료 버튼 클릭<br>2. 결과 화면 확인                               | 세션 상태가 completed로 변경, 점수 표시                   | High     |
| FT-8  | 팀스페이스 생성                  | 로그인 상태            | 1. 팀스페이스 생성 페이지 이동<br>2. 이름, 설명 입력<br>3. 생성 버튼 클릭 | 팀스페이스 생성, 초대 코드 발급                           | High     |
| FT-9  | 팀스페이스 초대 코드로 참가      | 로그인 상태, 초대 코드 | 1. 초대 링크 접속<br>2. 참가 버튼 클릭                                    | 팀스페이스 멤버로 추가됨                                  | High     |
| FT-10 | 팀스페이스 세션 공유             | 팀스페이스 멤버        | 1. 팀스페이스 내 세션 목록 확인                                           | 팀 멤버의 세션이 목록에 표시됨                            | Medium   |
| FT-11 | 케이스 스터디 조회               | 비로그인 가능          | 1. 케이스 스터디 목록 접속<br>2. 상세 페이지 진입                         | 목록 및 상세 정상 표시                                    | Medium   |
| FT-12 | 케이스 스터디 조회수/면접수 증가 | 비로그인 가능          | 1. 케이스 스터디 상세 페이지 진입<br>2. 면접 시작                         | `increment_view_count`, `increment_interview_count` 정상  | High     |
| FT-13 | Google 로그인                    | 비로그인 상태          | 1. 로그인 버튼 클릭<br>2. Google OAuth 진행                               | 로그인 성공, `handle_new_user` 트리거 정상 실행           | High     |
| FT-14 | 카테고리 목록 조회               | 로그인 상태            | 1. 면접 유형 선택 화면 접속                                               | 카테고리 목록 정상 반환                                   | Medium   |

---

## 보안 테스트

> anon key로 직접 Supabase에 접근하여 RLS 정책이 올바르게 차단하는지 확인합니다.
> 테스트 도구: Supabase Dashboard SQL Editor 또는 `curl`로 REST API 직접 호출

| #    | 테스트 시나리오                                     | 테스트 방법                                                                        | 예상 결과                              | 우선순위 |
| ---- | --------------------------------------------------- | ---------------------------------------------------------------------------------- | -------------------------------------- | -------- |
| SC-1 | anon key로 `question_cache` 테이블 읽기 시도        | `curl` 또는 Supabase JS Client(anon key)로<br>`GET /rest/v1/question_cache`        | 빈 배열 반환 또는 403 (RLS 차단)       | High     |
| SC-2 | anon key로 `question_cache` 테이블 쓰기 시도        | `POST /rest/v1/question_cache` body에 임의 데이터                                  | 403 또는 INSERT 거부                   | High     |
| SC-3 | anon key로 `users` 테이블 전체 SELECT 시도          | `GET /rest/v1/users`                                                               | 빈 배열 (전체 공개 SELECT 정책 삭제됨) | High     |
| SC-4 | anon key로 `users` 테이블 INSERT 시도               | `POST /rest/v1/users` body에 임의 사용자 데이터                                    | INSERT 거부 (과잉 INSERT 정책 삭제됨)  | High     |
| SC-5 | anon key로 `questions` 테이블 INSERT 시도           | `POST /rest/v1/questions` (Authorization: anon key)                                | INSERT 거부 (authenticated만 허용)     | Medium   |
| SC-6 | authenticated user로 `questions` INSERT 시도        | 로그인 후 Supabase JS Client로<br>`supabase.from('questions').insert({...})`       | INSERT 허용 (WITH CHECK(true))         | Medium   |
| SC-7 | authenticated user로 다른 사용자 데이터 UPDATE 시도 | 로그인 후<br>`supabase.from('users').update({name:'hack'}).eq('id', '다른유저ID')` | UPDATE 거부 (`auth.uid() = id` 정책)   | High     |

---

## vector 검색 테스트

> `vector` 확장이 `extensions` 스키마로 이동된 후 임베딩 관련 기능이 정상 동작하는지 확인합니다.

| #    | 테스트 시나리오                           | 테스트 단계                                                                   | 예상 결과                                                         | 우선순위 |
| ---- | ----------------------------------------- | ----------------------------------------------------------------------------- | ----------------------------------------------------------------- | -------- |
| VT-1 | 시맨틱 캐시 검색 (find_similar_questions) | 1. 이전에 생성된 질문이 있는 카테고리 선택<br>2. 동일 조건으로 질문 생성 요청 | 캐시 히트 시 기존 질문 반환, 미스 시 새 질문 생성                 | High     |
| VT-2 | 중복 질문 체크 (check_question_duplicate) | 1. 기존 질문과 유사한 질문 생성 시도                                          | 중복 감지 시 대체 질문 생성                                       | Medium   |
| VT-3 | 캐시 쿼리 검색 (search_cached_queries)    | 1. 동일한 면접 조건으로 질문 생성 2회 요청                                    | 2회차에 캐시된 쿼리 매칭, 기존 질문 재활용                        | Medium   |
| VT-4 | vector 연산자 정상 동작                   | Supabase SQL Editor에서<br>`SELECT extensions.vector(ARRAY[1,2,3]::float8[])` | 에러 없이 vector 타입 반환 (`OPERATOR(extensions.<=>)` 정상 작동) | Low      |

---

## 엣지 케이스 테스트

| #    | 시나리오                                       | 테스트 단계                                                     | 예상 결과                                        | 우선순위 |
| ---- | ---------------------------------------------- | --------------------------------------------------------------- | ------------------------------------------------ | -------- |
| EC-1 | 비로그인 사용자의 면접 세션 생성 (게스트 모드) | 1. 로그아웃 상태<br>2. 게스트 면접 시작<br>3. 질문 생성 및 답변 | 게스트 세션 정상 생성 (API가 supabaseAdmin 사용) | High     |
| EC-2 | 비로그인 사용자의 즐겨찾기 시도                | 1. 로그아웃 상태<br>2. 즐겨찾기 버튼 클릭                       | 로그인 유도 또는 적절한 에러 메시지              | Medium   |
| EC-3 | 로그인 후 게스트 세션 claim                    | 1. 게스트로 면접 완료<br>2. Google 로그인<br>3. 세션 claim      | 게스트 세션이 로그인 사용자에게 귀속됨           | Medium   |
| EC-4 | 다른 사용자의 세션 접근 시도                   | 1. 사용자 A로 로그인<br>2. 사용자 B의 세션 ID로 API 호출        | 접근 거부 또는 빈 결과 (API 레벨 권한 체크)      | Low      |
| EC-5 | 동시 다수 RPC 호출 (increment 함수)            | 1. 케이스 스터디 페이지에서 빠르게 새로고침 반복                | 조회수가 정확하게 증가, 에러 없음                | Low      |

---

## 성능 테스트

| #    | 측정 항목                              | 측정 방법                                       | 기준값                       | 우선순위 |
| ---- | -------------------------------------- | ----------------------------------------------- | ---------------------------- | -------- |
| PT-1 | vector 검색 응답 시간 (스키마 이동 후) | 질문 생성 API 호출 시간 측정 (DevTools Network) | 기존 대비 10% 이상 저하 없음 | Low      |
| PT-2 | 마이그레이션 후 질문 생성 전체 시간    | 면접 시작부터 질문 표시까지 소요 시간           | 기존과 동일 수준 (< 10초)    | Low      |

---

## 회귀 테스트

> 보안 변경이 기존 기능에 영향을 주지 않았는지 확인하는 핵심 흐름 테스트

| #    | 기능                    | 테스트 단계                                                                           | 예상 결과                     | 우선순위 |
| ---- | ----------------------- | ------------------------------------------------------------------------------------- | ----------------------------- | -------- |
| RT-1 | Google 로그인 전체 흐름 | 1. 로그아웃<br>2. Google 로그인<br>3. 메인 페이지 도달 확인                           | 정상 로그인, 사용자 정보 표시 | High     |
| RT-2 | 면접 전체 흐름 (E2E)    | 1. 카테고리 선택<br>2. 질문 생성<br>3. 답변 제출<br>4. 피드백 확인<br>5. 세션 완료    | 전체 흐름 에러 없이 완료      | High     |
| RT-3 | 케이스 스터디 전체 흐름 | 1. 케이스 스터디 목록<br>2. 상세 페이지<br>3. 면접 시작<br>4. 조회수/면접수 확인      | RPC 함수 정상 동작            | Medium   |
| RT-4 | 팀스페이스 전체 흐름    | 1. 팀스페이스 생성<br>2. 초대 코드 복사<br>3. 다른 계정으로 참가<br>4. 세션 공유 확인 | 팀 기능 정상 동작             | Medium   |

---

## 테스트 실행 가이드

### 사전 준비

1. 마이그레이션이 적용된 Supabase 프로젝트에서 테스트
2. 로컬 개발 서버 실행: `npm run dev`
3. 테스트용 Google 계정 2개 준비 (다른 사용자 테스트용)

### 보안 테스트 실행 방법

anon key 직접 접근 테스트는 다음 방법 중 택 1:

**방법 A: curl**

```bash
# anon key로 question_cache 읽기 시도
curl 'https://<project-ref>.supabase.co/rest/v1/question_cache?select=*' \
  -H "apikey: <anon-key>" \
  -H "Authorization: Bearer <anon-key>"
```

**방법 B: Supabase SQL Editor**

```sql
-- RLS 정책 확인
SELECT tablename, policyname, permissive, roles, cmd, qual, with_check
FROM pg_policies
WHERE schemaname = 'public'
ORDER BY tablename, policyname;
```

**방법 C: Supabase JS Client (브라우저 콘솔)**

```javascript
// anon key 클라이언트로 직접 테스트
const { data, error } = await supabase.from("question_cache").select("*");
console.log({ data, error }); // data: [] 또는 error 확인
```

### 테스트 순서

1. **High 우선순위** 먼저 실행 (SC-1 ~ SC-4, FT-1, FT-2, FT-5, FT-7 ~ FT-9, FT-12, FT-13, VT-1, EC-1, RT-1, RT-2)
2. **Medium 우선순위** 실행
3. **Low 우선순위** 실행
4. 실패 시 이슈 번호와 함께 기록
