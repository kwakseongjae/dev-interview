# QA Checklist - #61 질문 DB 중복 적재 취약점 수정

> Generated by qa-generator agent
> Date: 2026-03-17

## 테스트 요약

- **총 테스트 케이스**: 22개
- **우선순위별**: High 10, Medium 8, Low 4
- **예상 테스트 시간**: 40분

---

## 기능 테스트

| #    | 테스트 시나리오                                 | 사전 조건                                                            | 테스트 단계                                                       | 예상 결과                                                                                                                                     | 우선순위 |
| ---- | ----------------------------------------------- | -------------------------------------------------------------------- | ----------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------- | -------- |
| FT-1 | 일반 면접 세션 생성 시 질문 중복 방지           | 로그인 상태, 기존 질문 DB에 "React useMemo에 대해 설명해주세요" 존재 | 1. 검색 페이지에서 "React 면접 질문" 입력<br>2. 면접 시작         | 세션 정상 생성됨, questions 테이블에 동일 content_normalized의 질문이 중복 삽입되지 않음                                                      | High     |
| FT-2 | 동일 질문 재입력 시 기존 질문 ID 재사용         | FT-1 완료 상태                                                       | 1. 동일 쿼리로 새 면접 세션 생성<br>2. session_questions 확인     | 새 질문 행이 추가되지 않고 기존 question_id가 session_questions에 연결됨                                                                      | High     |
| FT-3 | 케이스 스터디 세션 생성                         | 로그인 상태, seed questions가 있는 케이스 스터디 존재                | 1. 케이스 스터디 페이지 접속<br>2. "면접 시작" 클릭               | 세션 정상 생성, seed questions가 upsert로 저장됨, 중복 질문 없음                                                                              | High     |
| FT-4 | POST /api/questions 중복 질문 생성 시도         | DB에 특정 질문 존재                                                  | 1. POST /api/questions로 동일 content 전송                        | 200 응답 + `{ duplicate: true }` 플래그와 함께 기존 질문 반환                                                                                 | High     |
| FT-5 | 대소문자만 다른 질문 중복 방지                  | DB에 "React란 무엇인가요?" 존재                                      | 1. "react란 무엇인가요?"로 질문 생성 시도                         | content_normalized가 동일하므로 중복으로 처리, 새 행 미생성                                                                                   | High     |
| FT-6 | 공백 차이만 있는 질문 중복 방지                 | DB에 "JavaScript 클로저를 설명해주세요" 존재                         | 1. "JavaScript 클로저를 설명해주세요"(다중 공백)로 질문 생성 시도 | 정규화 후 동일한 content_hash 생성, 중복으로 처리                                                                                             | High     |
| FT-7 | 시맨틱 캐시 저장 (storeQuestionsWithEmbeddings) | Claude가 새 질문 생성                                                | 1. 카테고리 검색 후 부족분 Claude 생성 트리거<br>2. 질문 저장     | upsert로 저장, RPC 중복 체크 누락분도 content_hash로 DB 레벨에서 차단                                                                         | Medium   |
| FT-8 | normalizeQuestionContent 정규화 일관성          | N/A                                                                  | 1. JS 함수와 PostgreSQL 함수에 동일 입력 전달<br>2. 결과 비교     | `normalizeQuestionContent("  Hello   World  ")` === `"hello world"`, DB의 `lower(trim(regexp_replace(content, '\s+', ' ', 'g')))` 결과와 동일 | High     |

---

## 엣지 케이스 테스트

| #    | 시나리오                                             | 테스트 단계                                                | 예상 결과                                                                                      | 우선순위 |
| ---- | ---------------------------------------------------- | ---------------------------------------------------------- | ---------------------------------------------------------------------------------------------- | -------- |
| EC-1 | 빈 문자열 질문                                       | 1. content가 빈 문자열인 질문 생성 시도                    | 400 에러 반환 ("질문 내용은 필수입니다")                                                       | Medium   |
| EC-2 | 공백만 있는 질문                                     | 1. content가 " " (공백만)인 질문 생성 시도                 | trim 후 빈 문자열로 판단, 400 에러 반환                                                        | Medium   |
| EC-3 | 특수문자 포함 질문                                   | 1. 따옴표, 백슬래시, 유니코드 이모지 포함 질문 생성        | 정상 저장, content_hash 정상 생성                                                              | Medium   |
| EC-4 | 매우 긴 질문 (2000자 한계)                           | 1. 2000자 길이의 질문 생성<br>2. 동일 내용으로 재생성 시도 | 첫 번째는 정상 저장, 두 번째는 중복으로 처리                                                   | Low      |
| EC-5 | 동시 요청으로 동일 질문 생성 (Race Condition)        | 1. 두 개의 동시 세션 생성 요청이 동일 질문 포함            | UNIQUE(content_hash) 제약으로 한 건만 저장, 나머지는 ignoreDuplicates로 충돌 없이 기존 ID 조회 | High     |
| EC-6 | content_normalized는 같지만 원본 content가 다른 질문 | 1. "React 설명" vs "react 설명" 두 질문 순차 생성          | content_hash가 동일하여 두 번째는 중복 처리, 원본 content는 첫 번째 것 유지                    | Medium   |

---

## 회귀 테스트

| #    | 기능                           | 테스트 단계                                                                         | 예상 결과                                                      | 우선순위 |
| ---- | ------------------------------ | ----------------------------------------------------------------------------------- | -------------------------------------------------------------- | -------- |
| RT-1 | 면접 세션 생성 및 진행         | 1. 검색 페이지에서 질문 검색<br>2. 면접 시작<br>3. 질문별 답변 작성<br>4. 면접 완료 | 전체 플로우 정상 작동, 질문 표시/답변 저장/완료 처리 모두 정상 | High     |
| RT-2 | 케이스 스터디 면접 진행        | 1. 케이스 스터디 선택<br>2. 면접 시작<br>3. 질문 답변<br>4. 완료                    | seed questions 기반 세션 정상 생성 및 진행                     | Medium   |
| RT-3 | 세션 히스토리 조회             | 1. 면접 완료 후 히스토리 페이지 접속<br>2. 세션 상세 조회                           | 질문-세션 연결 정상, 기존 세션 데이터 손상 없음                | Medium   |
| RT-4 | 즐겨찾기 기능                  | 1. 질문 즐겨찾기 추가<br>2. 즐겨찾기 목록 확인                                      | FK 재지정된 질문의 즐겨찾기 정상 작동                          | Medium   |
| RT-5 | 팀 스페이스 세션 공유          | 1. 팀 스페이스에서 면접 시작<br>2. 팀 멤버가 공유 세션 확인                         | 세션 자동 공유 정상, 질문 데이터 정상 표시                     | Low      |
| RT-6 | 질문 검색 (GET /api/questions) | 1. 카테고리별 질문 검색<br>2. 키워드 검색<br>3. 페이지네이션                        | 검색 결과 정상, 중복 제거 후에도 기존 질문 조회 가능           | Low      |

---

## 데이터 무결성 테스트

| #    | 검증 항목                            | 검증 방법 (SQL)                                                                                                                                                                                   | 예상 결과                              | 우선순위 |
| ---- | ------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------- | -------- |
| DI-1 | content_hash 컬럼 존재 및 UNIQUE     | `SELECT count(*) FROM information_schema.columns WHERE table_name='questions' AND column_name='content_hash';`<br>`SELECT indexdef FROM pg_indexes WHERE indexname='idx_questions_content_hash';` | 컬럼 존재, UNIQUE 인덱스 존재          | High     |
| DI-2 | content_hash가 모든 행에 값 존재     | `SELECT count(*) FROM questions WHERE content_hash IS NULL;`                                                                                                                                      | 0건 (generated column이므로 NULL 불가) | High     |
| DI-3 | content_hash 중복 없음               | `SELECT content_hash, count(*) FROM questions GROUP BY content_hash HAVING count(*) > 1;`                                                                                                         | 0건 (UNIQUE 제약)                      | High     |
| DI-4 | content_normalized 정규화 일관성     | `SELECT count(*) FROM questions WHERE content_normalized != lower(trim(regexp_replace(content, '\s+', ' ', 'g')));`                                                                               | 0건 (모든 행이 통일된 정규화 적용)     | Medium   |
| DI-5 | FK 무결성 (session_questions)        | `SELECT count(*) FROM session_questions sq LEFT JOIN questions q ON sq.question_id = q.id WHERE q.id IS NULL;`                                                                                    | 0건 (고아 참조 없음)                   | High     |
| DI-6 | FK 무결성 (answers)                  | `SELECT count(*) FROM answers a LEFT JOIN questions q ON a.question_id = q.id WHERE q.id IS NULL;`                                                                                                | 0건 (고아 참조 없음)                   | High     |
| DI-7 | FK 무결성 (favorites)                | `SELECT count(*) FROM favorites f LEFT JOIN questions q ON f.question_id = q.id WHERE q.id IS NULL;`                                                                                              | 0건 (고아 참조 없음)                   | Medium   |
| DI-8 | 마이그레이션 후 총 질문 수 감소 확인 | `SELECT count(*) FROM questions;`                                                                                                                                                                 | 기존 446개 → 352개 (94개 중복 삭제)    | Low      |

---

## 테스트 실행 가이드

1. 로컬 개발 서버 실행: `npm run dev`
2. 데이터 무결성 테스트(DI-\*)는 Supabase SQL Editor 또는 `npx supabase db` CLI로 실행
3. High 우선순위 테스트부터 순서대로 진행
4. EC-5 (Race Condition) 테스트는 두 개의 브라우저 탭에서 동시에 면접 시작으로 검증
5. 각 테스트 결과를 체크박스에 기록
6. 실패 시 이슈 번호와 함께 기록
