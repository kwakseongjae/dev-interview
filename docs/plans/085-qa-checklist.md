# QA Checklist — #85 캐시 미스 해소 + #88 비기술 질문 필터링

> Generated by qa-generator agent
> Date: 2026-04-03

---

## 테스트 요약

- **총 테스트 케이스**: 24개
- **우선순위별**: High 9, Medium 10, Low 5
- **예상 테스트 시간**: 45분
- **대상 파일**:
  - `src/lib/question-cache.ts` (Tiered Fallback 캐시 검색)
  - `src/lib/question-history.ts` (히스토리 카운트 / stale 조회)
  - `src/lib/validation.ts` (비기술 콘텐츠 필터 `isNonDevContent`)

---

## 기능 테스트

### 캐시 히트 및 동적 풀 크기

| #    | 테스트 시나리오           | 사전 조건                              | 테스트 단계                                                        | 예상 결과                                                                   | 우선순위 |
| ---- | ------------------------- | -------------------------------------- | ------------------------------------------------------------------ | --------------------------------------------------------------------------- | -------- |
| FT-1 | 신규 사용자 캐시 검색     | 히스토리 0개인 사용자 로그인           | 1. "React 프론트엔드 면접" 검색<br>2. 콘솔 로그의 `poolLimit` 확인 | poolLimit = count \* 4 (기본 multiplier), Tier 1에서 충분한 결과 반환       | High     |
| FT-2 | 반복 사용자 동적 풀 확장  | 히스토리 50개 이상인 사용자            | 1. "React 프론트엔드 면접" 검색<br>2. 콘솔 로그의 `poolLimit` 확인 | poolLimit이 기본값(count\*4)보다 크고 historyBuffer가 반영됨, 최대 200 이하 | High     |
| FT-3 | poolLimit 상한 200 제한   | 히스토리 500개 이상인 사용자           | 1. count=5로 검색 실행<br>2. 콘솔 로그 확인                        | poolLimit이 200을 초과하지 않음                                             | Medium   |
| FT-4 | 히스토리 필터링 정상 작동 | 동일 카테고리 질문 20개 이상 학습 이력 | 1. 동일 카테고리 재검색<br>2. 반환된 질문 확인                     | 이전에 본 질문이 결과에 포함되지 않음 (stale 제외)                          | High     |

### 비기술 콘텐츠 필터링 (`isNonDevContent`)

| #    | 테스트 시나리오                   | 사전 조건                                                                    | 테스트 단계                                                        | 예상 결과                                                                                | 우선순위 |
| ---- | --------------------------------- | ---------------------------------------------------------------------------- | ------------------------------------------------------------------ | ---------------------------------------------------------------------------------------- | -------- |
| FT-5 | 순수 비기술 질문 필터링           | DB에 "알바 경험이 없는데 이 일을 잘 할 수 있다고 생각하시나요?" 류 질문 존재 | 1. 해당 카테고리 검색 실행<br>2. 결과에 비기술 질문 포함 여부 확인 | `isNonDevContent`가 true 반환, 결과에서 제외됨                                           | High     |
| FT-6 | 기술 맥락 포함 질문 통과          | DB에 "카페 POS 시스템의 동시성 문제를 어떻게 해결하시겠습니까?" 류 질문 존재 | 1. 검색 실행<br>2. 해당 질문이 결과에 포함되는지 확인              | DEV_CONTEXT_KEYWORDS("시스템", "동시성") 매칭으로 `isNonDevContent` = false, 결과에 포함 | High     |
| FT-7 | 콘솔 로그의 nonDevFiltered 카운트 | DB에 비기술 질문이 섞여 있는 카테고리                                        | 1. 검색 실행<br>2. 콘솔 로그의 `tier1.nonDevFiltered` 확인         | 필터링된 비기술 질문 수가 정확히 표시됨                                                  | Low      |

### 인접 카테고리 확장 (Tier 2)

| #     | 테스트 시나리오           | 사전 조건                                         | 테스트 단계                                                                              | 예상 결과                                                                     | 우선순위 |
| ----- | ------------------------- | ------------------------------------------------- | ---------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------- | -------- |
| FT-8  | Tier 2 확장 트리거        | FRONTEND 카테고리 질문 부족 (count > Tier 1 결과) | 1. 히스토리가 많아 FRONTEND 풀 소진된 상태에서 검색<br>2. 콘솔 로그 `tier2Expanded` 확인 | CS, ARCHITECTURE, NETWORK 인접 카테고리에서 추가 질문 확보, tier2Expanded > 0 | High     |
| FT-9  | Tier 2 중복 제거          | Tier 1과 Tier 2에 동일 질문 가능성                | 1. 인접 카테고리 확장 검색<br>2. 결과에서 중복 ID 확인                                   | existingIds Set으로 중복 제거, 같은 질문이 2번 나오지 않음                    | Medium   |
| FT-10 | 인접 카테고리 매핑 정확성 | BACKEND 카테고리 검색                             | 1. "백엔드 개발자 면접" 검색<br>2. Tier 2 확장 시 참조되는 카테고리 확인                 | DATABASE, SYSTEM_DESIGN, DEVOPS, CS가 인접으로 사용됨                         | Medium   |

### 오래된 질문 재활용 (Tier 3)

| #     | 테스트 시나리오             | 사전 조건                                          | 테스트 단계                                           | 예상 결과                                                         | 우선순위 |
| ----- | --------------------------- | -------------------------------------------------- | ----------------------------------------------------- | ----------------------------------------------------------------- | -------- |
| FT-11 | Stale 질문 재활용           | 14일 전에 본 질문이 히스토리에 존재, Tier 1+2 부족 | 1. 검색 실행<br>2. 콘솔 로그 `tier3Recycled` 확인     | 14일 이상 경과한 질문이 재활용되어 결과에 포함, tier3Recycled > 0 | High     |
| FT-12 | 13일 전 질문은 재활용 안 됨 | 13일 전에 본 질문 존재                             | 1. 검색 실행<br>2. 해당 질문이 결과에 포함되는지 확인 | staleDays=14 기준 미달, 재활용 대상에서 제외                      | Medium   |
| FT-13 | 비로그인 사용자 Tier 3 스킵 | userId 없는 상태                                   | 1. 비로그인 상태에서 검색<br>2. 콘솔 로그 확인        | historySize=0, staleContents 비어있음, Tier 3 로직 실행 안 됨     | Medium   |

### 병렬 히스토리 쿼리

| #     | 테스트 시나리오                | 사전 조건     | 테스트 단계                                            | 예상 결과                                                                                                     | 우선순위 |
| ----- | ------------------------------ | ------------- | ------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------- | -------- |
| FT-14 | Promise.all 3개 쿼리 병렬 실행 | 로그인 사용자 | 1. 검색 실행<br>2. 서버 로그에서 쿼리 실행 타이밍 확인 | getQuestionHistoryCount, getQuestionHistory, getStaleQuestionHistory가 병렬 실행됨 (순차 대비 응답 시간 단축) | Medium   |

---

## 엣지 케이스

| #    | 시나리오                                  | 테스트 단계                                                        | 예상 결과                                                                                          |
| ---- | ----------------------------------------- | ------------------------------------------------------------------ | -------------------------------------------------------------------------------------------------- |
| EC-1 | 히스토리 0개 (신규 사용자)                | 1. 신규 계정으로 로그인<br>2. 첫 검색 실행                         | historySize=0, dynamicMultiplier=baseMultiplier(4), seenContents/staleContents 비어있음, 정상 검색 |
| EC-2 | 히스토리 200개 이상 (헤비 유저)           | 1. 다량의 히스토리 보유 사용자<br>2. 검색 실행                     | poolLimit이 200 cap에 도달, 히스토리 limit=200으로 조회, Tier 2/3까지 확장 가능                    |
| EC-3 | 모든 카테고리 소진 (Tier 1+2+3 전부 부족) | 1. 매우 많은 히스토리로 모든 관련 질문을 본 사용자<br>2. 검색 실행 | pool.length < count인 채로 반환, Claude가 부족분 생성 (캐시 부분 히트)                             |
| EC-4 | 비로그인 사용자 검색                      | 1. 로그아웃 상태에서 면접 검색                                     | userId 없음 → 히스토리 조회 스킵, poolLimit=count\*4 고정, 필터 없이 캐시 결과 반환                |
| EC-5 | 카테고리 매핑 불가 쿼리                   | 1. "asdfgh 면접 질문" 같이 카테고리 매핑 안 되는 쿼리 입력         | extractCategoriesFromQuery 빈 배열 반환 → searchCachedQuestions 빈 배열 반환 → Claude 전량 생성    |
| EC-6 | DB 조회 오류 발생                         | 1. DB 연결 문제 시뮬레이션                                         | try-catch에서 에러 로깅 후 빈 배열 반환, Claude fallback 생성으로 사용자 경험 유지                 |

---

## 회귀 테스트

| #    | 기능                                          | 테스트 단계                                                                         | 예상 결과                                                             |
| ---- | --------------------------------------------- | ----------------------------------------------------------------------------------- | --------------------------------------------------------------------- |
| RT-1 | 기존 정상 쿼리 검색 ("프론트엔드 3년차 면접") | 1. 기존에 잘 작동하던 쿼리 입력<br>2. 5개 질문 생성 요청                            | 기존과 동일한 품질의 면접 질문 반환, 에러 없음                        |
| RT-2 | 레퍼런스 URL 기반 질문 생성                   | 1. 기술 블로그 URL 입력하여 질문 생성<br>2. 결과 확인                               | URL 기반 질문 생성 정상 작동, 캐시 로직이 레퍼런스 플로우에 영향 없음 |
| RT-3 | 질문 저장 (storeQuestionsWithEmbeddings)      | 1. 새 질문 생성<br>2. DB에 저장 확인                                                | 임베딩 생성 + 중복 체크 + 저장 정상 작동 (변경 없는 함수)             |
| RT-4 | favorite_count 기반 가중 샘플링               | 1. favorite_count가 높은 질문과 낮은 질문이 혼재된 카테고리 검색<br>2. 여러 번 반복 | favorite_count 높은 질문이 더 자주 선택됨 (통계적 편향 확인)          |

---

## 성능 테스트

| #    | 측정 항목                                         | 측정 방법                               | 기준값                                                                                         |
| ---- | ------------------------------------------------- | --------------------------------------- | ---------------------------------------------------------------------------------------------- |
| PT-1 | 캐시 검색 응답 시간 (신규 사용자)                 | DevTools Network 탭, 검색 API 응답 시간 | < 500ms (히스토리 쿼리 없으므로 빠름)                                                          |
| PT-2 | 캐시 검색 응답 시간 (반복 사용자, 히스토리 100개) | DevTools Network 탭, 검색 API 응답 시간 | < 800ms (Promise.all 병렬 쿼리 포함)                                                           |
| PT-3 | DB 쿼리 수 (로그인 사용자, Tier 1만 충분)         | 서버 로그 또는 Supabase Dashboard       | categories SELECT 1회 + Promise.all 3회(count, history, stale) + questions SELECT 1회 = 총 5회 |
| PT-4 | DB 쿼리 수 (Tier 2 확장 시)                       | 서버 로그                               | 위 5회 + Tier 2 questions SELECT 1회 = 총 6회                                                  |

---

## 테스트 실행 가이드

1. 로컬 개발 서버 실행: `npm run dev`
2. High 우선순위 테스트부터 순서대로 진행
3. 콘솔 로그 확인을 위해 브라우저 DevTools Console 및 터미널 서버 로그 모니터링
4. 캐시 검색 결과 로그 형식 참고:
   ```
   캐시 검색 결과: {
     query, primaryCategories, historySize, poolLimit,
     tier1: { fetched, afterFilter, nonDevFiltered },
     tier2Expanded, tier3Recycled,
     finalPool, requested
   }
   ```
5. 각 테스트 결과를 체크박스에 기록
6. 실패 시 이슈 번호와 함께 기록
