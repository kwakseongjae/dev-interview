# [#69] QA Checklist: 회원탈퇴 기능 추가 (Soft Delete)

> Generated by qa-generator agent
> Date: 2026-03-29

---

## 테스트 요약

- **총 테스트 케이스**: 22개
- **우선순위별**: High 10, Medium 8, Low 4
- **예상 테스트 시간**: 40분

---

## 기능 테스트

| #     | 테스트 시나리오                          | 사전 조건                      | 테스트 단계                                                                | 예상 결과                                                                                  | 우선순위 |
| ----- | ---------------------------------------- | ------------------------------ | -------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------ | -------- |
| FT-1  | 설정 페이지 접근 (로그인)                | 로그인 상태                    | 1. 홈페이지 헤더의 Settings 아이콘 클릭                                    | `/settings` 페이지로 이동, 계정 정보(닉네임, 이메일, 가입일) 표시                          | High     |
| FT-2  | 설정 페이지 접근 (비로그인)              | 비로그인 상태                  | 1. 브라우저에서 `/settings` 직접 접속                                      | `/auth` 페이지로 리다이렉트                                                                | High     |
| FT-3  | 로그아웃 버튼                            | 로그인 + 설정 페이지           | 1. "로그아웃" 버튼 클릭                                                    | 로그아웃 처리 후 홈페이지로 이동                                                           | Medium   |
| FT-4  | 탈퇴 확인 모달 열기                      | 설정 페이지                    | 1. 위험 영역의 "회원탈퇴" 버튼 클릭                                        | 탈퇴 확인 모달 표시, 경고 문구 + 확인 입력란 + 사유 선택 표시                              | High     |
| FT-5  | "탈퇴합니다" 미입력 시 버튼 비활성화     | 탈퇴 모달 오픈                 | 1. 확인 입력란을 비워둠<br>2. "회원탈퇴" 버튼 확인                         | 버튼이 disabled 상태, 클릭 불가                                                            | High     |
| FT-6  | "탈퇴합니다" 정확히 입력 시 버튼 활성화  | 탈퇴 모달 오픈                 | 1. 확인 입력란에 "탈퇴합니다" 입력                                         | "회원탈퇴" 버튼이 활성화됨                                                                 | High     |
| FT-7  | 잘못된 텍스트 입력 시 버튼 비활성화 유지 | 탈퇴 모달 오픈                 | 1. "탈퇴 합니다" (공백 포함) 또는 "탈퇴" 등 입력                           | 버튼이 disabled 상태 유지                                                                  | Medium   |
| FT-8  | 탈퇴 사유 선택 (선택사항)                | 탈퇴 모달 오픈                 | 1. 사유 칩 중 하나 클릭<br>2. 선택된 칩 다시 클릭                          | 클릭 시 선택 표시(destructive 스타일), 재클릭 시 선택 해제                                 | Medium   |
| FT-9  | 회원탈퇴 실행                            | "탈퇴합니다" 입력 완료         | 1. "회원탈퇴" 버튼 클릭                                                    | 로딩 스피너 표시 → 탈퇴 처리 → 로그아웃 → 홈페이지로 리다이렉트 (`/?deleted=true`)         | High     |
| FT-10 | 탈퇴 완료 배너 표시                      | 탈퇴 처리 완료 후              | 1. 홈페이지 리다이렉트 확인                                                | "회원탈퇴가 완료되었습니다. 그동안 이용해주셔서 감사합니다." 배너 표시, 5초 후 자동 사라짐 | High     |
| FT-11 | 탈퇴 후 URL 파라미터 제거                | 탈퇴 완료 후 홈페이지          | 1. URL 확인                                                                | `?deleted=true` 파라미터가 `history.replaceState`로 제거되어 URL이 `/`로 깨끗함            | Low      |
| FT-12 | 탈퇴 후 재로그인 차단                    | 탈퇴 완료된 계정               | 1. 동일 Google 계정으로 로그인 시도                                        | ban_duration 적용으로 로그인 불가 (Supabase Auth 에러)                                     | High     |
| FT-13 | 탈퇴 사유와 함께 탈퇴                    | "탈퇴합니다" 입력 + 사유 선택  | 1. 사유 "더 이상 서비스를 이용하지 않아요" 선택<br>2. "회원탈퇴" 버튼 클릭 | 탈퇴 성공, DB에 deletion_reason 저장됨                                                     | Medium   |
| FT-14 | 탈퇴 사유 없이 탈퇴                      | "탈퇴합니다" 입력, 사유 미선택 | 1. "회원탈퇴" 버튼 클릭                                                    | 탈퇴 성공, deletion_reason이 null로 저장                                                   | Medium   |

---

## 엣지 케이스

| #    | 시나리오                            | 테스트 단계                                                                          | 예상 결과                                                                       | 우선순위 |
| ---- | ----------------------------------- | ------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------- | -------- |
| EC-1 | 이미 탈퇴한 계정으로 중복 탈퇴 요청 | 1. API `POST /api/auth/delete-account`를 직접 호출 (이미 deleted_at이 설정된 사용자) | 400 에러: "이미 탈퇴 처리된 계정입니다"                                         | High     |
| EC-2 | 비인증 상태에서 탈퇴 API 호출       | 1. 로그아웃 상태에서 `POST /api/auth/delete-account` 호출                            | 401 에러: "인증이 필요합니다"                                                   | High     |
| EC-3 | ban 처리 실패 시 롤백               | 1. supabaseAdmin.auth.admin.updateUserById 실패 시뮬레이션                           | soft delete가 롤백됨 (deleted_at, deletion_reason이 null로 복원), 500 에러 반환 | Medium   |
| EC-4 | 탈퇴 처리 중 모달 닫기 방지         | 1. "회원탈퇴" 버튼 클릭 (로딩 중)<br>2. 모달 배경 또는 "취소" 클릭 시도              | isDeleting 상태에서 모달 닫기 차단, 취소 버튼 비활성화                          | Medium   |
| EC-5 | 미들웨어: banned 사용자 쿠키 정리   | 1. banned 사용자가 남아있는 `sb-*` 쿠키로 페이지 접근                                | getUser 실패 감지 → `sb-*` 쿠키 삭제 → `/?deleted=true`로 리다이렉트            | High     |
| EC-6 | 탈퇴 사유 길이 제한 (500자)         | 1. API body에 500자 초과 reason 전달                                                 | reason이 500자로 잘려서 저장됨 (slice 처리)                                     | Low      |
| EC-7 | 탈퇴 API에 body 없이 호출           | 1. `POST /api/auth/delete-account` body 없이 호출                                    | 에러 없이 reason: null로 정상 처리                                              | Low      |
| EC-8 | 설정 페이지 로딩 중 화면            | 1. `/settings` 접속 시 API 응답 대기 중                                              | 로딩 스피너 표시, 깜빡임 없음                                                   | Low      |

---

## 회귀 테스트

| #    | 기능                           | 테스트 단계                                                            | 예상 결과                                                                    | 우선순위 |
| ---- | ------------------------------ | ---------------------------------------------------------------------- | ---------------------------------------------------------------------------- | -------- |
| RT-1 | Google 로그인 (정상 사용자)    | 1. 정상 사용자로 Google 로그인                                         | 로그인 성공, 홈페이지 정상 표시                                              | High     |
| RT-2 | 미들웨어 정상 동작 (일반 유저) | 1. 정상 로그인된 사용자가 페이지 탐색                                  | 기존과 동일하게 세션 유지, 쿠키 정상 작동                                    | High     |
| RT-3 | 면접 세션 생성 및 진행         | 1. 로그인 후 질문 검색<br>2. 면접 세션 시작 및 답변                    | 면접 세션 정상 생성 및 진행                                                  | Medium   |
| RT-4 | 홈페이지 헤더 레이아웃         | 1. 로그인 상태에서 홈페이지 확인<br>2. 비로그인 상태에서 홈페이지 확인 | Settings 아이콘 추가 후에도 헤더 레이아웃 정상, 로그인/비로그인 UI 구분 정상 | Medium   |
