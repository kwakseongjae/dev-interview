# QA Checklist — #60 수동 질문 시드 기능 (Bulk Question Seeding)

> Generated by qa-generator agent
> Date: 2026-03-17

---

## 테스트 요약

- **총 테스트 케이스**: 24개
- **우선순위별**: High 8, Medium 11, Low 5
- **예상 테스트 시간**: 45분

---

## 기능 테스트

| #     | 테스트 시나리오                        | 사전 조건                                    | 테스트 단계                                                                     | 예상 결과                                                            | 우선순위 |
| ----- | -------------------------------------- | -------------------------------------------- | ------------------------------------------------------------------------------- | -------------------------------------------------------------------- | -------- |
| FT-1  | 전체 분석 흐름 실행                    | Supabase MCP 연결, DB에 카테고리 데이터 존재 | 1. `/seed-questions` 실행                                                       | 카테고리별 분포 테이블 출력 → 추천 목록 출력 → 사용자 확인 대기      | High     |
| FT-2  | 특정 카테고리 직접 생성                | Supabase MCP 연결                            | 1. `/seed-questions frontend 5` 실행                                            | 분석 단계 건너뛰고 FRONTEND 질문 5개 생성 → JSON 저장 → DB INSERT    | High     |
| FT-3  | 트렌드 토픽 직접 생성                  | Supabase MCP 연결                            | 1. `/seed-questions --trend rag-pipeline 10` 실행                               | is_trending=true, trend_topic="rag-pipeline"인 질문 10개 생성        | High     |
| FT-4  | 균등 배분 모드                         | Supabase MCP 연결                            | 1. `/seed-questions --balance 100` 실행                                         | 카테고리 분석 후 부족한 카테고리 위주로 100개 배분 생성              | Medium   |
| FT-5  | JSON 파일 저장 확인                    | FT-2 실행 완료                               | 1. `data/seeds/` 디렉토리에서 파일 확인                                         | `seed-{날짜}-frontend.json` 파일 존재, 올바른 JSON 형식              | High     |
| FT-6  | DB INSERT 확인                         | FT-2 실행 완료                               | 1. Supabase에서 `SELECT * FROM questions ORDER BY created_at DESC LIMIT 5` 실행 | 생성된 질문이 DB에 존재, is_verified=false                           | High     |
| FT-7  | 텍스트 중복 체크 동작                  | DB에 이미 시드된 질문 존재                   | 1. 동일 카테고리로 `/seed-questions frontend 5` 재실행                          | 중복된 질문은 스킵, 리포트에 중복 스킵 수 표시                       | High     |
| FT-8  | 결과 리포트 출력                       | FT-2 실행 완료                               | 1. 실행 완료 후 리포트 확인                                                     | 생성 수, 저장 수, 중복 스킵 수가 테이블 형태로 표시                  | Medium   |
| FT-9  | storeQuestionsWithEmbeddings 반환 타입 | 기존 질문 생성 API 동작 상태                 | 1. `/api/questions/generate` 호출하여 질문 생성                                 | 기존 플로우 정상 동작, `.catch()` 패턴에서 반환값 무시해도 에러 없음 | High     |
| FT-10 | content_normalized 정규화 규칙         | -                                            | 1. 특수문자, 대문자 포함 질문 생성<br>2. DB에서 content_normalized 컬럼 확인    | 소문자 변환 + 특수문자 제거된 값 저장 (한글/영문/숫자/공백만 유지)   | Medium   |

---

## 엣지 케이스

| #    | 시나리오                    | 테스트 단계                                                      | 예상 결과                                                    | 우선순위 |
| ---- | --------------------------- | ---------------------------------------------------------------- | ------------------------------------------------------------ | -------- |
| EC-1 | Supabase MCP 미연결 상태    | 1. Supabase MCP 비활성화<br>2. `/seed-questions frontend 5` 실행 | 에러 메시지 출력 후 중단, 크래시 없음                        | High     |
| EC-2 | 존재하지 않는 카테고리 지정 | 1. `/seed-questions nonexistent 5` 실행                          | 카테고리 ID 조회 실패, 해당 항목 스킵 안내 메시지 출력       | Medium   |
| EC-3 | count가 0 또는 음수         | 1. `/seed-questions frontend 0` 실행                             | 질문 생성 없이 종료, 에러 없음                               | Low      |
| EC-4 | count가 50 초과             | 1. `/seed-questions frontend 60` 실행                            | NFR-1에 따라 최대 50개로 제한하거나 배치 분할 처리           | Medium   |
| EC-5 | 빈 DB (카테고리/질문 0개)   | 1. 질문이 없는 상태에서 `/seed-questions` 실행                   | 분석 결과에 질문 수 0 표시, 모든 카테고리 추천               | Medium   |
| EC-6 | 모든 질문이 중복인 경우     | 1. 이미 시드된 질문과 동일한 내용으로 재시드 시도                | `{ stored: 0, duplicates: N }` 반환, "모든 질문이 중복" 로그 | Medium   |
| EC-7 | INSERT 부분 실패            | 1. 일부 질문의 category_id가 유효하지 않은 상태에서 INSERT       | 에러 로그 출력, 성공/실패 항목 구분하여 리포트               | Low      |

---

## UI/UX 테스트

커맨드 라인 기반이므로 출력 형식 및 사용자 상호작용 검증.

| #    | 확인 항목                   | 검증 방법                                  | 예상 결과                                                          | 우선순위 |
| ---- | --------------------------- | ------------------------------------------ | ------------------------------------------------------------------ | -------- |
| UX-1 | 카테고리 분포 테이블 가독성 | `/seed-questions` 실행 후 Step 1 출력 확인 | 카테고리명, 질문 수, 비율이 정렬된 테이블로 출력                   | Medium   |
| UX-2 | 추천 목록 명확성            | Step 2 출력 확인                           | 카테고리/트렌드, 추천 수, 추천 이유가 명확하게 표시                | Medium   |
| UX-3 | 사용자 확인 단계 존재       | 전체 흐름 실행 시 Step 2 이후 관찰         | 사용자 확인 요청 메시지 출력, 수정 가능 안내                       | Medium   |
| UX-4 | 결과 리포트 형식            | 시딩 완료 후 리포트 확인                   | 항목별 생성/저장/중복 스킵 수가 테이블로 출력, 저장 파일 경로 표시 | Low      |

---

## 회귀 테스트

| #    | 기능                                     | 테스트 단계                                                                  | 예상 결과                                              | 우선순위 |
| ---- | ---------------------------------------- | ---------------------------------------------------------------------------- | ------------------------------------------------------ | -------- |
| RT-1 | 기존 질문 생성 API                       | 1. 검색 페이지에서 "프론트엔드 면접" 입력<br>2. 질문 생성 대기               | 기존과 동일하게 질문 생성, 캐시 히트/미스 정상 동작    | High     |
| RT-2 | storeQuestionsWithEmbeddings 기존 호출   | 1. `/api/questions/generate` 호출<br>2. 새 질문 생성 확인<br>3. DB 저장 확인 | `.catch()` 패턴에서 반환값 변경에 영향 없음, 정상 저장 | High     |
| RT-3 | 시맨틱 캐시 검색 (searchCachedQuestions) | 1. 이전에 캐시된 카테고리로 검색<br>2. 캐시 히트 확인                        | 기존 캐시 검색 로직 정상 동작                          | Medium   |

---

## 성능 테스트

| #    | 측정 항목                    | 측정 방법                                        | 기준값                                           | 우선순위 |
| ---- | ---------------------------- | ------------------------------------------------ | ------------------------------------------------ | -------- |
| PT-1 | 카테고리 분석 쿼리 응답 시간 | Supabase MCP `execute_sql` 실행 시간 관찰        | < 1초 (NFR-2)                                    | Medium   |
| PT-2 | 빌드 사이즈 변화             | `npm run build` 출력에서 번들 사이즈 확인        | question-cache.ts 증가 < 1KB (인터페이스 추가만) | Low      |
| PT-3 | 기존 생성 API 응답 시간      | `/api/questions/generate` 호출 후 응답 시간 측정 | 기존 대비 변화 없음                              | Low      |

---

## 데이터 무결성 테스트

| #    | 확인 항목                  | 테스트 단계                                                                | 예상 결과                                         | 우선순위 |
| ---- | -------------------------- | -------------------------------------------------------------------------- | ------------------------------------------------- | -------- |
| DI-1 | JSON 파일과 DB 데이터 일치 | 1. 시드 실행 후 JSON 파일 내용 확인<br>2. DB에서 동일 질문 조회<br>3. 비교 | content, hint, category, difficulty 일치          | Medium   |
| DI-2 | is_verified 기본값         | 1. 시드된 질문의 is_verified 컬럼 확인                                     | 모든 시드 질문이 `is_verified = false`            | Medium   |
| DI-3 | 난이도 분포                | 1. 시드 결과 JSON에서 difficulty 분포 확인                                 | 대략 EASY 20%, MEDIUM 50%, HARD 30% (권장치 근사) | Low      |
| DI-4 | category_id FK 정합성      | 1. DB에서 시드된 질문의 category_id 확인<br>2. categories 테이블과 JOIN    | 모든 category_id가 유효한 카테고리 레코드 참조    | Medium   |

---

## 테스트 실행 가이드

1. Supabase MCP 연결 확인 (필수 전제 조건)
2. High 우선순위 테스트부터 순서대로 진행
3. FT-1 ~ FT-3으로 기본 플로우 검증 후 엣지 케이스 진행
4. RT-1 ~ RT-2 회귀 테스트로 기존 기능 영향 없음 확인
5. 각 테스트 결과를 아래 체크박스에 기록
6. 실패 시 이슈 번호와 함께 기록

### 테스트 결과 기록

- [ ] FT-1 ~ FT-10 기능 테스트
- [ ] EC-1 ~ EC-7 엣지 케이스
- [ ] UX-1 ~ UX-4 UI/UX 테스트
- [ ] RT-1 ~ RT-3 회귀 테스트
- [ ] PT-1 ~ PT-3 성능 테스트
- [ ] DI-1 ~ DI-4 데이터 무결성 테스트
