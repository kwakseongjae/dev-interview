# #68 - Soft Auth Gate (비로그인 면접 시작 유도) QA Checklist

> Generated by qa-generator agent
> Date: 2026-03-29

## 테스트 요약

- **총 테스트 케이스**: 24개
- **우선순위별**: High 9, Medium 10, Low 5
- **예상 테스트 시간**: 40분

---

## 기능 테스트

> "시작하기" 버튼 클릭 시 로그인 상태에 따른 분기 처리가 정상 동작하는지 확인합니다.

| #     | 테스트 시나리오                   | 사전 조건                     | 테스트 단계                                                                                                   | 예상 결과                                                                 | 우선순위 |
| ----- | --------------------------------- | ----------------------------- | ------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------- | -------- |
| FT-1  | 로그인 유저 면접 시작             | Google 로그인 상태            | 1. /search에서 질문 생성<br>2. "시작하기" 버튼 클릭                                                           | 모달 없이 바로 면접 시작 (기존 플로우 유지)                               | High     |
| FT-2  | 비로그인 유저 모달 표시           | 비로그인 상태                 | 1. /search에서 질문 생성<br>2. "시작하기" 버튼 클릭                                                           | LoginPromptModal 표시, type="start-interview" 콘텐츠 노출                 | High     |
| FT-3  | 모달 콘텐츠 확인                  | 비로그인 상태, 모달 열림      | 1. 모달 내용 확인                                                                                             | 제목: "로그인하고 면접을 시작하세요", 혜택 3개 리스트, 두 개 버튼 표시    | Medium   |
| FT-4  | "Google로 시작하기" 클릭          | 비로그인 상태, 모달 열림      | 1. "Google로 시작하기" 버튼 클릭                                                                              | sessionStorage에 질문 저장 후 Google OAuth 리다이렉트                     | High     |
| FT-5  | "비회원으로 계속하기" 클릭        | 비로그인 상태, 모달 열림      | 1. "비회원으로 계속하기" 버튼 클릭                                                                            | 모달 닫힘, 게스트 세션으로 면접 즉시 시작                                 | High     |
| FT-6  | OAuth 복귀 후 자동 면접 시작      | OAuth 로그인 완료             | 1. 비로그인 상태에서 모달 "Google로 시작하기" 클릭<br>2. Google 로그인 완료<br>3. /search?restore=true로 복귀 | 로딩 오버레이 표시 → 자동 세션 생성 → 면접 페이지 이동                    | High     |
| FT-7  | OAuth 복귀 시 로딩 UI 표시        | OAuth 복귀 중                 | 1. /search?restore=true 접속                                                                                  | "면접을 준비하고 있어요..." 로딩 오버레이 표시                            | Medium   |
| FT-8  | sessionStorage 질문 데이터 검증   | 비로그인 상태, 질문 생성 완료 | 1. "Google로 시작하기" 클릭<br>2. DevTools > Application > sessionStorage 확인                                | pendingInterview 키에 questions, query, interviewTypeCode, timestamp 저장 | Medium   |
| FT-9  | OAuth 복귀 후 sessionStorage 정리 | OAuth 복귀, 세션 생성 성공    | 1. 면접 페이지 이동 후<br>2. DevTools에서 sessionStorage 확인                                                 | pendingInterview 키 삭제됨                                                | Medium   |
| FT-10 | 모달 X 버튼으로 닫기              | 비로그인 상태, 모달 열림      | 1. 모달의 X 버튼 또는 배경 클릭                                                                               | 모달 닫힘, 면접 시작되지 않음                                             | Low      |

---

## OAuth Restore 플로우 테스트

> Google 로그인 후 /search?restore=true로 복귀하는 전체 플로우를 검증합니다.

| #    | 테스트 시나리오                         | 사전 조건           | 테스트 단계                                                                                    | 예상 결과                                                 | 우선순위 |
| ---- | --------------------------------------- | ------------------- | ---------------------------------------------------------------------------------------------- | --------------------------------------------------------- | -------- |
| RF-1 | 정상 restore 플로우 (end-to-end)        | 비로그인 상태       | 1. 질문 생성<br>2. 시작하기 클릭<br>3. Google로 시작하기<br>4. 로그인 완료<br>5. 복귀 확인     | 자동으로 면접 시작, /interview 페이지로 이동              | High     |
| RF-2 | interviewTypeCode 보존 확인             | 면접 유형 선택 상태 | 1. 면접 유형 선택 후 질문 생성<br>2. 전체 restore 플로우 진행<br>3. 생성된 세션 확인           | interviewTypeCode가 세션에 올바르게 전달됨                | Medium   |
| RF-3 | 트렌드 토픽 플로우 (홈 > 검색 > 모달)   | 비로그인 상태       | 1. 홈에서 트렌드 토픽 클릭<br>2. /search로 이동, 질문 자동 생성<br>3. 시작하기 > Google로 시작 | 트렌드 토픽 질문이 sessionStorage에 저장되고 restore 정상 | High     |
| RF-4 | restore 중복 실행 방지 (hasRestoredRef) | OAuth 복귀 직후     | 1. /search?restore=true로 접속<br>2. React re-render 발생                                      | autoStart가 1회만 실행됨 (중복 세션 생성 없음)            | Medium   |

---

## 엣지 케이스 테스트

| #    | 시나리오                                | 테스트 단계                                                                                          | 예상 결과                                                                 | 우선순위 |
| ---- | --------------------------------------- | ---------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------- | -------- |
| EC-1 | sessionStorage 데이터 10분 초과 (stale) | 1. sessionStorage에 10분 이전 timestamp로 pendingInterview 수동 설정<br>2. /search?restore=true 접속 | pendingInterview 삭제됨, URL에서 restore=true 제거, 일반 검색 페이지 표시 | High     |
| EC-2 | sessionStorage 데이터 없이 restore 접속 | 1. sessionStorage 비운 상태<br>2. /search?restore=true 직접 접속                                     | URL에서 restore=true 제거, 일반 검색 페이지로 폴백                        | Medium   |
| EC-3 | sessionStorage 비활성화 환경            | 1. 브라우저에서 sessionStorage 차단<br>2. 비로그인으로 "Google로 시작하기" 클릭                      | savePendingInterview 에러 발생 시에도 OAuth 리다이렉트 진행               | Low      |
| EC-4 | OAuth 도중 뒤로가기                     | 1. 비로그인에서 "Google로 시작하기" 클릭<br>2. Google 로그인 화면에서 뒤로가기                       | /search 페이지로 복귀, sessionStorage에 데이터 남아있음, 재시도 가능      | Medium   |
| EC-5 | restore 중 세션 생성 API 실패           | 1. 네트워크 오류 또는 서버 에러 상태<br>2. /search?restore=true로 복귀                               | 에러 로그 출력, pendingInterview 삭제, URL 정리, 로딩 해제                | Medium   |
| EC-6 | sessionStorage에 손상된 JSON 저장       | 1. sessionStorage의 pendingInterview에 잘못된 JSON 수동 설정<br>2. /search?restore=true 접속         | JSON.parse catch 블록에서 처리, pendingInterview 삭제, 정상 폴백          | Low      |

---

## UI/UX 테스트

| #    | 확인 항목                     | 검증 방법                     | 예상 결과                                                         | 우선순위 |
| ---- | ----------------------------- | ----------------------------- | ----------------------------------------------------------------- | -------- |
| UI-1 | 모달 혜택 리스트 표시         | 비로그인 상태에서 모달 열기   | 체크마크(gold) + 3개 혜택 문구 정렬 표시                          | Low      |
| UI-2 | 로딩 오버레이 디자인          | OAuth 복귀 시 로딩 화면 확인  | 반투명 배경 + Loader2 스피너 + "면접을 준비하고 있어요..." 텍스트 | Low      |
| UI-3 | 모달 버튼 로딩 상태           | "Google로 시작하기" 클릭 직후 | 버튼에 Loader2 스피너 표시, 버튼 비활성화                         | Medium   |
| UI-4 | 모달 반응형 레이아웃 (모바일) | 모바일 뷰포트에서 모달 열기   | sm:max-w-md 적용, 모바일에서 전체 너비, 버튼 터치 영역 적절       | Low      |

---

## 회귀 테스트

> 기존 플로우가 Soft Auth Gate 추가로 영향받지 않았는지 확인합니다.

| #    | 기능                            | 테스트 단계                                                     | 예상 결과                                         | 우선순위 |
| ---- | ------------------------------- | --------------------------------------------------------------- | ------------------------------------------------- | -------- |
| RT-1 | 로그인 유저 기존 면접 플로우    | 1. Google 로그인<br>2. 질문 생성<br>3. "시작하기" 클릭          | 모달 없이 바로 면접 시작 (기존과 동일)            | High     |
| RT-2 | 게스트 면접 완료 후 저장 모달   | 1. 비로그인으로 면접 시작 (비회원으로 계속하기)<br>2. 면접 완료 | 기존 interview/complete 모달 정상 동작            | Medium   |
| RT-3 | 기존 LoginPromptModal 타입 동작 | 1. interview 타입 모달 트리거<br>2. complete 타입 모달 트리거   | 기존 type별 콘텐츠 정상 표시                      | Medium   |
| RT-4 | 찜하기 로그인 유도              | 1. 비로그인 상태에서 질문 찜하기 클릭                           | "로그인이 필요합니다" alert 표시 (기존 동작 유지) | Low      |

---

## 테스트 실행 가이드

1. 로컬 개발 서버 실행: `npm run dev`
2. 비로그인 테스트: 시크릿/프라이빗 브라우저 창 사용 권장
3. High 우선순위 테스트부터 순서대로 진행
4. sessionStorage 확인: DevTools > Application > Session Storage > localhost
5. OAuth 테스트 시 Supabase Redirect URLs에 `http://localhost:3000/**` 등록 확인
6. 각 테스트 결과를 체크박스에 기록
7. 실패 시 이슈 번호와 함께 기록
