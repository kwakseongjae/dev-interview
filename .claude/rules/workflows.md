# 워크플로우 가이드

> CLAUDE.md의 상세 워크플로우 설명

---

## 전체 워크플로우

### 1. 이슈 시작

```bash
/issue-start "Add dark mode toggle"
```

**동작**:

- GitHub 이슈 생성
- 브랜치 생성 (`{type}/{issue_number}-{slug}`)
- 브랜치 체크아웃
- 라벨 자동 추가 (`type:*`, `area:*`, `priority:*`)

### 2. 작업 계획 수립

```bash
/task-init                           # 이슈만 분석
/task-init "추가 설명"               # 이슈 + 텍스트
/task-init [이미지 첨부]             # 이슈 + 이미지
```

**동작** (자세한 내용: `.claude/skills/task-init/SKILL.md`):

1. **컨텍스트 수집**:
   - GitHub 이슈 분석 (`gh issue view`)
   - 사용자 입력 처리 (텍스트/이미지)
   - 통합 분석 및 충돌 해결
2. 코드베이스 탐색 (Explore agent, thoroughness: medium)
3. 필요 시 웹 검색 (라이브러리, 패턴, best practices)
4. 계획 문서 작성 (`docs/plans/{issue_number}-{slug}.md`)
5. 서브에이전트 설정 (vercel-react-best-practices 자동 적용)
6. 사용자 승인 대기

**입력 옵션**:

- **텍스트**: "Header에 토글 추가, localStorage 저장"
- **이미지**: 디자인 목업, UI 스크린샷, 에러 화면
- **둘 다**: "이 디자인 사용 + 단축키 추가" [이미지]

**생성되는 것**:

- 계획 문서 (10개 섹션, 사용자 컨텍스트 반영)
- 스펙 문서 (조건부, API/스키마 관련 이슈만) → `docs/specs/{issue}/spec.md`
- 서브에이전트: `react-developer`, `code-reviewer`, `test-writer`, `doc-writer`

**스펙 문서 트리거 조건**:

- 이슈 레이블: `api`, `spec`, `schema`, `data-model`
- 이슈 키워드: "API", "endpoint", "스키마", "데이터 모델"

### 3. 구현 진행

```bash
# 계획에 따라 코드 작성
# 서브에이전트가 Vercel React Best Practices 자동 적용
```

**자동 적용되는 규칙**:

- `async-parallel`, `bundle-barrel-imports` (CRITICAL)
- `server-cache-react`, `server-serialization` (HIGH)
- `rerender-memo`, `rerender-functional-setstate` (MEDIUM)

### 4. 작업 완료 및 문서화

```bash
/task-done
```

**동작** (자세한 내용: `.claude/skills/task-done/SKILL.md`):

1. 계획 문서 찾기 (`docs/plans/{issue_number}-*.md`)
2. 품질 게이트 검증:
   ```bash
   npm run build        # 필수
   npx tsc --noEmit    # 필수
   npm run lint        # 필수
   ```
3. 변경 사항 수집:
   - 변경된 파일 (`git diff --name-only main...HEAD`)
   - 커밋 히스토리
   - 성능 영향 분석
4. Implementation Summary 생성 및 추가
5. **📋 Spec 문서 업데이트** (스펙 문서가 있는 경우):
   - `status: draft → implemented`
   - Implementation Notes 섹션 추가
   - 계획 문서와 상호 참조
6. **🧪 QA Checklist 자동 생성** (`qa-generator` 에이전트):
   - 기능 테스트 시나리오
   - 엣지 케이스 테스트
   - UI/UX 테스트
   - 회귀 테스트
7. 서브에이전트 정리
8. PR 가이드 제공

**실패 시**:

- 품질 게이트 실패 시 작업 완료 불가
- 구체적인 에러 메시지 제공
- 수정 후 다시 `/task-done` 실행

### 5. 커밋

```bash
/commit
```

**동작**:

- Conventional Commits 형식
- `Closes #{issue_number}` 자동 추가

**형식**:

```
type(scope): subject

Detailed description if needed.

Closes #25
```

### 6. PR 생성

```bash
/pr
```

**동작**:

- PR 제목: 이슈 제목 기반
- PR 본문:
  - Summary (계획 문서 기반)
  - Test plan
  - 변경 파일 목록
- 베이스 브랜치: `main` (자동)

### 6-1. 빠른 완료 (원스텝)

```bash
/commit-push-pr
```

**동작**:

- `/commit` + 푸시 + `/pr`을 한 번에 실행
- 빌드 검증 → 커밋 생성 → 브랜치 푸시 → PR 생성
- `--draft` 옵션으로 Draft PR 생성 가능

**언제 사용?**:

- 작업이 완료되어 빠르게 PR까지 올리고 싶을 때
- 단계별 확인이 필요 없는 신뢰할 수 있는 변경사항

---

## 병렬 작업 (Git Worktree)

Git Worktree를 사용하여 동일한 레포지토리의 여러 브랜치를 동시에 작업할 수 있습니다.

**사용 시나리오**:

- 여러 이슈를 동시에 작업
- 긴급 버그 수정 중 다른 브랜치 확인 필요
- 코드 리뷰 중 별도 브랜치에서 테스트

### Worktree 워크플로우 (권장)

**issue-start + worktree skill 사용**:

```bash
# 1. 이슈 생성 + 브랜치 생성
/issue-start "Add feature X"

# 2. 워크트리로 전환 (선택적)
# "워크트리로 만들어줘" 또는 "Create worktree" 요청
# → worktree skill이 자동 적용됨

# 3. 작업 계획 수립
/task-init

# 4. 구현 진행
# (코드 작성)

# 5. 작업 완료 및 문서화
/task-done

# 6. 커밋
/commit

# 7. PR 생성
/pr

# 8. Worktree 정리 (PR 머지 후)
/worktree-done
```

**동작 설명**:

**`/issue-start` + worktree skill**:

- `/issue-start`로 이슈 및 브랜치 생성
- "워크트리로 만들어줘" 요청 시 worktree skill 적용
- 워크트리 디렉토리: `mochabun-{type}-{issue_number}-{slug}`
- 자동으로 워크트리로 이동
- main 디렉토리는 main 브랜치로 복귀

**`/worktree-done`**:

- 워크트리 제거
- main 브랜치로 복귀
- 최신 변경사항 pull

### 전통적인 방식과의 비교

**전통적인 브랜치 방식**:

```bash
/issue-start → 코드 작성 → 다른 이슈로 전환 시 stash 필요
```

- 한 번에 하나의 이슈만 작업 가능
- 이슈 전환 시 stash/unstash 필요
- 단일 작업 디렉토리

**Worktree 방식**:

```bash
/issue-start → "워크트리로" 요청 → 새 디렉토리 → 다른 터미널에서 다른 이슈 작업 가능
```

- 여러 이슈를 동시에 작업 가능
- Stash 불필요
- 터미널/윈도우 전환만으로 이슈 전환
- 독립적인 작업 디렉토리들

### 병렬 작업 예시

```bash
# 터미널 1: Feature 작업
cd ~/projects/mochabun
/issue-start "Add pricing table"
# "워크트리로 만들어줘" 요청
# → 워크트리 생성: mochabun-feat-42-add-pricing-table
/task-init
# (작업 진행)

# 터미널 2: 긴급 버그 수정 (동시에 진행)
cd ~/projects/mochabun
/issue-start "Fix payment bug"
# "워크트리로 만들어줘" 요청
# → 워크트리 생성: mochabun-fix-43-fix-payment-bug
/task-init
# (작업 진행)

# 두 작업을 독립적으로 진행 가능
# 각 워크트리는 독립적인 node_modules, .env, working tree
```

### 수동 Worktree 명령어 (고급)

커스텀 커맨드 사용이 어려운 경우:

```bash
# Worktree 생성
git worktree add ../mochabun-feat-26 feat/26-another-task

# Worktree 목록 확인
git worktree list

# Worktree 제거
git worktree remove ../mochabun-feat-26
```

**자세한 가이드**: [docs/WORKTREE_GUIDE.md](../../docs/WORKTREE_GUIDE.md)

---

## 팁 & 트릭

### 빠른 피드백 루프

```bash
# 구현 중 자주 실행
npm run build && npx tsc --noEmit && npm run lint
```

### 계획 문서 업데이트

계획 문서는 살아있는 문서입니다:

- 구현 중 새로운 발견 → 계획 문서의 "Deviations" 섹션에 메모
- `/task-done`이 자동으로 요약 생성

### 서브에이전트 활용

- `react-developer`: "이 컴포넌트를 최적화해줘"
- `code-reviewer`: "이 코드 리뷰해줘"
- `test-writer`: "이 함수에 대한 테스트 작성해줘"
- `spec-documenter`: API/스키마 관련 스펙 문서 작성
- `qa-generator`: 작업 완료 시 자동으로 QA 체크리스트 생성

### Spec 커맨드 활용

API/스키마 관련 작업 시:

```bash
/spec generate    # 현재 이슈 기반 스펙 문서 수동 생성
/spec validate    # 스펙과 구현 일치 검증
/spec sync        # 구현 결과를 스펙에 반영
```

스펙 문서 위치: `docs/specs/{issue_number}/spec.md`

### 품질 게이트 실패 시

1. 에러 메시지 확인
2. 문제 수정
3. 검증 재실행
4. 통과 후 `/task-done` 재시도

---

**마지막 업데이트**: 2026-02-04
